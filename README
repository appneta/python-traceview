How to install into a pylons(/django?) app
===============================================

-- see tender!

Q: how to publish?

A: for testing: python setup.py register -r internal sdist upload -r internal
A: for release: python setup.py register -r tracelytics sdist upload -r tracelytics

Make sure your .pypirc is up to date--check /home/dan/.pypirc
