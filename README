How to install into a pylons(/django?) app
===============================================

-- see tender!

Q: how to publish?

A: for testing: python setup.py register -r internal bdist_egg upload -r internal
A: for release: python setup.py register -r tracelytics bdist_egg upload -r tracelytics

Make sure your .pypirc is up to date--check /home/dan/.pypirc
